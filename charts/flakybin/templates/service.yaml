apiVersion: v1
kind: Service
metadata:
  name: {{ include "flakybin.fullname" . }}
  labels:
    {{- include "flakybin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "flakybin.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
